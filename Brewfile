brew "git"
brew "bc"
brew "zsh"
brew "neovim"      # LazyVim용 최신 버전
brew "stow"        # Dotfile 관리용
brew "ripgrep"     # Neovim Telescope용
brew "fd"          # Neovim용
brew "fzf"
brew "tmux"
brew "bat"

if OS.mac?
end

# Linux(Ubuntu)에서만 필요한 것 (보통은 공통 패키지로 충분)
if OS.linux?
  brew "gcc"       # 일부 플러그인 컴파일용
end
